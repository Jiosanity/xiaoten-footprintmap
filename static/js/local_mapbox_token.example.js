// local_mapbox_token.example.js
// 用法：复制此文件为 `local_mapbox_token.js`，将你的 Mapbox token 填入字符串中
// 注意：不要把 `local_mapbox_token.js` 提交到 Git 仓库 — 请把它加入 `.gitignore`。

window.__MAPBOX_TOKEN_OVERRIDE = 'PUT_YOUR_MAPBOX_TOKEN_HERE';
console.log('LOCAL_MAPBOX_TOKEN_LOADED');
